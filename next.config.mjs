/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['images.unsplash.com', 'cdn.dribbble.com', 'img.freepik.com','images5.alphacoders.com'],
    }
};

export default nextConfig;